# ma factory

```bash
ma doctor
ma ship C:/repo "goal" --verify "pytest -q" --workers 0
ma watch PROJECT_ID --follow
ma cancel PROJECT_ID
ma report PROJECT_ID
ma usage PROJECT_ID
ma clean PROJECT_ID
```

Events: `~/.ma/events/<id>.jsonl`  
Cancel marker: `~/.ma/events/<id>.cancel`
